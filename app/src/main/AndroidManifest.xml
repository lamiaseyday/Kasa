<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kasayonetimi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity android:name=".ListDenemeActivity"
            android:theme="@style/AppTheme">



        </activity>
        <activity android:name=".ParolaUnuttumActivity" />
        <activity
            android:name=".ProcessActivity"
            android:theme="@style/AppThemeUc" />
        <activity
            android:name=".SelectedActivity"
            android:theme="@style/AppThemeiki" />
        <activity
            android:name=".SearchActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppThemeiki">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>